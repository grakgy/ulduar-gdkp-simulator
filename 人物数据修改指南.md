# 人物数据修改指南

人物数据分散在四张 CSV 表中，同一个人物必须使用完全相同的 `player_id` 关联。修改后保存文件、刷新游戏并重新开团即可生效。

## 修改现有人物

### 1. `Players_Public.csv`：招募时看得到的信息

- `name`：人物名字。
- `class`：职业，必须使用现有职业名称。
- `signup_spec` / `signup_role`：报名专精和职责。职责只能写 `坦克`、`治疗`、`近战DPS`、`远程DPS`。
- `signup_item_level`：基础装等。建议普通玩家 212–224，较强角色 225–228，229 已经非常少见，230–232 只留给接近毕业的打工号。
- `claimed_offspec`：公开副修；多个专精用 `、` 分隔，没有就留空。
- `progress_display`、`public_economy_claim`、`whisper_pool`：公开自述。密语候选之间用 `|` 分隔。

### 2. `Players_Hidden.csv`：玩家看不到的真实属性

战斗属性通常使用 0–100：

- `main_skill`：人物总体水平。
- `mechanics`：处理机制和打断的能力。
- `awareness`：观察环境、提前走位和救场能力。
- `stability`：发挥波动，越高越稳定。
- `teamwork`：配合和补位能力。
- `learning`：灭团后的学习速度。
- `pressure_resistance`：逆风时的抗压能力。
- `social_primary` / `social_secondary`：团队频道性格，例如责任型、压力怪、调解者、拱火者、玻璃心、沉默型。
- `base_leave_pct`：基础退团概率，建议 0–12；极端人物也不要轻易超过 18。
- `economy_type`：大老板、小老板、实力消费、毕业装党、武器饰品党、捡漏型、排骨党或口嗨消费。
- `wallet_gold`、`spend_willingness`、`bid_aggression`：资金、消费意愿和竞价风格。

顶级玩家不必同时拥有高装等。想做“操作弥补装备”的人物，可以给 216–222 装等，并把 `main_skill`、`mechanics`、`awareness` 和主修专精的 `skill` 提到 92–98。

### 3. `Player_Specs.csv`：每个专精的真实水平

每个可用专精占一行：

- `spec` / `role`：专精和对应职责。
- `skill`：这个专精的独立水平。
- `boss_experience`：对奥杜尔机制的熟悉程度。
- `publicly_claimed`：填 `是` 才会在 Boss 前的下拉框出现。

公开主修和公开副修都必须在此表各有一行。隐藏但未公开的专精可以保留数据，不过游戏不会允许团长切换过去。

### 4. `Player_Pool.csv`：本局会抽到的人物池

这里目前列出 40 个 `player_id`。想让新人物进入游戏，把新 ID 加进此表；想继续保持40人池，就同时删除一个旧 ID。

## 添加新人

例如添加 `P086`：

1. 在 `Players_Public.csv` 添加一行 `P086` 的公开资料。
2. 在 `Players_Hidden.csv` 添加一行 `P086` 的隐藏属性。
3. 在 `Player_Specs.csv` 至少添加一行 `P086` 的报名专精；有副修就继续添加。
4. 在 `Player_Pool.csv` 添加 `P086`。
5. 运行 `npm run verify`，再运行 `npm run build`。

注意不要在普通文字中直接输入英文逗号；字段里确实需要逗号时，要用英文双引号包住整个字段。`player_id` 不能重复，也不能只添加其中一张表。
