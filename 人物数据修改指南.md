# 人物数据修改指南

需要了解哪些 CSV 可以直接提交部署、哪些文件只是检查报告，请先看 `CSV直接维护与部署指南.md`。

人物数据主要维护在三张 CSV 表中，同一个人物必须使用完全相同的 `player_id` 关联。修改后请保存为 UTF-8、刷新游戏并重新开团。当前 `P081–P120` 是 40 名玩家自建人物，每局固定入池；系统再从随机人物中按本局 seed 抽取 10 名，组成 50 人选手池。

## 修改现有人物

### 1. `Players_Public.csv`：招募时看得到的信息

- `name`：人物名字。
- `class`：职业，必须使用现有职业名称。
- `signup_spec` / `signup_role`：报名专精和职责。职责只能写 `坦克`、`治疗`、`近战DPS`、`远程DPS`。
- `signup_item_level`：基础装等。建议普通玩家 212–224，较强角色 225–228，229 已经非常少见，230–232 只留给接近毕业的打工号。
- `claimed_offspec`：公开副修；多个专精用 `、` 分隔，没有就留空。
- `progress_display`、`public_economy_claim`、`whisper_pool`：公开自述。密语候选之间用 `|` 分隔。

### 2. `Players_Hidden.csv`：玩家看不到的真实属性

战斗属性通常使用 0–100：

- `main_skill`：人物总体水平。
- `mechanics`：处理机制和打断的能力。
- `awareness`：观察环境、提前走位和救场能力。
- `stability`：发挥波动，越高越稳定。
- `teamwork`：配合和补位能力。
- `learning`：灭团后的学习速度。
- `pressure_resistance`：逆风时的抗压能力。
- `social_primary` / `social_secondary`：团队频道性格，例如责任型、压力怪、调解者、拱火者、玻璃心、沉默型。
- `base_leave_pct`：基础退团概率，使用 0–1 小数，例如 `0.08` 表示 8%。
- `economy_type`：大老板、小老板、实力消费、毕业装党、武器饰品党、捡漏型、排骨党或口嗨消费。
- `wallet_gold`、`spend_willingness`、`bid_aggression`：资金、消费意愿和竞价风格。
- `source_type`：只能写 `随机生成` 或 `玩家自建`。
- `personality_type`、`strength_tags`、`weakness_tags`：维护人员看的简短人设、长处和短板；多个标签用 `|` 分隔。
- `special_rule`：人物专属规则的自然语言摘要，具体效果仍需在 `src/engine.ts` 接入。
- `leave_policy`：`正常`、`条件退团` 或 `永不主动退队`。
- `description`：完整隐藏人设，不会在招募页公开。

所有字段的类型、范围和用途都列在 `Players_Hidden_字段说明.csv`。推荐优先参照这张表维护，避免把百分比、金币和 0–100 属性混在一起。

顶级玩家不必同时拥有高装等。想做“操作弥补装备”的人物，可以给 216–222 装等，并把 `main_skill`、`mechanics`、`awareness` 和主修专精的 `skill` 提到 92–98。

### 3. `Player_Specs.csv`：每个专精的真实水平

每个可用专精占一行：

- `spec` / `role`：专精和对应职责。
- `skill`：这个专精的独立水平。
- `boss_experience`：对奥杜尔机制的熟悉程度。
- `publicly_claimed`：填 `是` 才会在 Boss 前的下拉框出现。

公开主修和公开副修都必须在此表各有一行。隐藏但未公开的专精可以保留数据，不过游戏不会允许团长切换过去。

`learning` 和 `boss_experience` 会共同影响二、三次尝试：经验越低、学习越高，灭团复盘后的成长越明显。因此小白不必永远是固定弱点，也不要用提高基础属性来代替学习曲线。

### 4. `Player_Pool.csv`：兼容保留，不再手工维护

运行时不再读取这张表。游戏会固定加入全部玩家自建人物，再从随机生成人物中按本局 seed 抽取 10 名，组成 50 人选手池；每次重新开团会更换随机人物。

## 添加新人

例如添加 `P121`：

1. 在 `Players_Public.csv` 添加一行 `P121` 的公开资料。
2. 在 `Players_Hidden.csv` 添加一行 `P121` 的隐藏属性，并把 `source_type` 写成 `玩家自建`。
3. 在 `Player_Specs.csv` 至少添加一行 `P121` 的报名专精；有副修就继续添加。
4. 不需要修改 `Player_Pool.csv`；只要隐藏表中的 `source_type` 是 `玩家自建`，人物就会固定进入每局池子。
5. 运行 `npm run verify`，再运行 `npm run build`。

注意不要在普通文字中直接输入英文逗号；字段里确实需要逗号时，要用英文双引号包住整个字段。`player_id` 不能重复，也不能只添加其中一张表。

## 扩展游戏语言

`Chat_Templates.csv` 使用三列：`scene`、`style_or_trait`、`template`。

- `scene` 目前支持 `报名`、`灭团`、`退团`、`拍卖`。
- `style_or_trait` 应与报名风格、`social_primary` 或 `social_secondary` 对应。
- 报名模板可使用 `{spec}` 和 `{offspec}` 占位符；纯 DPS 职业会自动省略专精，避免出现“邪DK 1”一类不自然密语。
- “报名”确实会显示在招募候选卡的密语中：随机人物按公开报名风格使用这里的模板；玩家自建人物优先使用 `Players_Public.csv` 中自己的 `whisper_pool`，从而保留个人口吻。
- 拍卖模板可使用 `{item}` 和 `{price}` 占位符；没有填写价格占位符时，系统仍会补上实际出价。
- 四种场景均已接入游戏：报名用于候选人密语，灭团用于战后复盘，退团用于散团前发言，拍卖用于竞价和流拍记录。
- 同一风格多写几句即可增加随机性，不需要改代码。建议每种场景的每种风格至少保留 15 条，`npm run verify` 会检查数量。
- `Chat_性格覆盖关系.csv` 列出了全部自建人物的主/副性格、灭团模板数、退团模板数和运行时使用方式；运行 `npm run chat:coverage` 可重新生成并检查是否有遗漏。`永不主动退队` 人物仍保留退团模板作数据完整性校验，但正常规则下不会主动触发。

## 批量导入人物 B

本次 `新增_自建人物Bxlsx.xlsx` 的 18 名人物已导入为 `P103–P120`；`P081–P102` 为上一批自建人物 A（含柠檬七喜）。需要按同一来源重新生成时运行 `npm run import:players-b`，然后依次运行 `npm run chat:coverage`、`npm run verify` 和 `npm run build`。导入脚本不会修改原始 Excel。

## 扩展战局结局

结局判定和文案统一维护在 `src/endings.ts`，结果页只负责展示。新增结局时应设置唯一 `id`、优先级、标题和正文；Boss 三灭结局必须使用 `bossId` 精确关联，不能依赖显示名称。修改后运行 `npm run verify`，确认每个 Boss 至少有 5 个专属结局且所有分支都有兜底文案。

## 扩展战斗记录文案

战斗记录里用于开场、击杀和灭团收尾的通用文案统一维护在 `Combat_Log_Templates.csv`，修改后无需再改组件代码。

- `opening`：战斗开始时的团队状态。
- `kill`：无减员击杀后的收尾。
- `kill_deaths`：有人倒地但最终击杀时的收尾。
- `wipe_fatal`：致命机制导致灭团。
- `wipe_attrition`：减员后输出或治疗缺口逐渐扩大。
- `wipe_enrage`：输出不足、最终狂暴或超时。

模板支持 `{boss}` 和 `{hp}` 占位符。每个 Boss 的具体机制事件仍维护在 `Boss_Events.csv`；前者负责魔兽团队语气和随机变化，后者负责技能名与机制结果。
